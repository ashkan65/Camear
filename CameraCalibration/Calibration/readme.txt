first run the recordframes use "C" to capture frames
then cd to Calibration/build and do the single camera calibration
run:
sudo ./single_calibration ../setting_left.xml
and
sudo ./single_calibration ../setting_right.xml

Now do the sterio calibration:




